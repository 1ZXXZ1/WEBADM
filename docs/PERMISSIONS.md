# Система прав и ролей

## Обзор

Samba AD Panel использует ролевую модель доступа (RBAC). Права назначаются через Python/FastAPI бэкенд и хранятся в PostgreSQL. Фронтенд получает права при авторизации и использует их для управления видимостью UI-элементов.

## Роли

| Роль | Описание | Уровень доступа |
|------|----------|----------------|
| **admin** | Администратор | Полный доступ ко всем функциям |
| **operator** | Оператор | Управление объектами AD, выполнение команд |
| **auditor** | Аудитор | Просмотр данных и журналов аудита |
| **viewer** | Наблюдатель | Минимальный доступ только для чтения |

## Права доступа (permissions)

### Пользователи
| Право | Описание |
|-------|----------|
| `user.list` | Просмотр списка пользователей |
| `user.create` | Создание пользователей |
| `user.update` | Обновление пользователей |
| `user.delete` | Удаление пользователей |
| `user.password` | Сброс паролей |
| `user.enable` | Включение/отключение учётных записей |

### Группы
| Право | Описание |
|-------|----------|
| `group.list` | Просмотр списка групп |
| `group.create` | Создание групп |
| `group.update` | Обновление групп |
| `group.delete` | Удаление групп |
| `group.members` | Управление членством в группах |

### Компьютеры
| Право | Описание |
|-------|----------|
| `computer.list` | Просмотр списка компьютеров |
| `computer.create` | Создание учётных записей компьютеров |
| `computer.delete` | Удаление компьютеров |

### Контакты
| Право | Описание |
|-------|----------|
| `contact.list` | Просмотр списка контактов |
| `contact.create` | Создание контактов |
| `contact.update` | Обновление контактов |
| `contact.delete` | Удаление контактов |

### Подразделения (OU)
| Право | Описание |
|-------|----------|
| `ou.list` | Просмотр списка OU |
| `ou.create` | Создание OU |
| `ou.delete` | Удаление OU |

### DNS
| Право | Описание |
|-------|----------|
| `dns.zonelist` | Просмотр списка DNS зон |
| `dns.records` | Управление DNS записями |

### GPO
| Право | Описание |
|-------|----------|
| `gpo.list` | Просмотр списка GPO |
| `gpo.manage` | Управление GPO |

### Домен
| Право | Описание |
|-------|----------|
| `domain.info` | Просмотр информации о домене |
| `domain.manage` | Управление доменом |

### Shell
| Право | Описание |
|-------|----------|
| `shell.execute` | Выполнение команд в терминале |
| `shell.projects` | Управление проектами |

### Batch / ETL
| Право | Описание |
|-------|----------|
| `batch.execute` | Выполнение пакетных операций |

### Управление
| Право | Описание |
|-------|----------|
| `mgmt.users.list` | Просмотр API пользователей |
| `mgmt.users.manage` | Управление API пользователями |
| `mgmt.keys.list` | Просмотр API ключей |
| `mgmt.keys.manage` | Управление API ключами |
| `mgmt.roles.list` | Просмотр ролей |
| `mgmt.audit.view` | Просмотр журналов аудита |

## Навигация и права

Элементы навигации связаны с правами. Если у пользователя нет нужного права, раздел отображается с иконкой замка и уменьшенной прозрачностью:

| Раздел | Необходимое право |
|--------|-------------------|
| Dashboard | (доступен всем) |
| Users | `user.list` |
| Groups | `group.list` |
| Computers | `computer.list` |
| Contacts | `contact.list` |
| OU | `ou.list` |
| DNS | `dns.zonelist` |
| GPO | `gpo.list` |
| Domain | `domain.info` |
| Shell | `shell.execute` |
| Shell Projects | `shell.execute` |
| Tasks (ETL) | `batch.execute` |
| Audit | `mgmt.audit.view` |
| Management | `mgmt.users.list` |

## Компонент RequirePermission

Компонент `RequirePermission` оборачивает контент и блокирует его при отсутствии прав:

```tsx
<RequirePermission permission="user.create">
  <Button onClick={handleCreate}>Создать пользователя</Button>
</RequirePermission>
```

Если у пользователя нет права `user.create`, кнопка будет заблокирована (заблюрена и неактивна).

## Проверка прав в коде

```typescript
// В Zustand store
const hasPermission = useAuthStore(state => state.hasPermission);
const isAdmin = useAuthStore(state => state.isAdmin);

// Проверка конкретного права
if (hasPermission('user.create')) {
  // Показать кнопку создания
}

// Проверка роли администратора
if (isAdmin()) {
  // Показать административные функции
}
```

> **Важно:** Права проверяются только на фронтенде (для управления UI). Бэкенд должен выполнять собственную проверку прав для каждого API-запроса. Фронтендная проверка не заменяет серверную!
